import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign up | Pets&Plus',
  description: 'Join our marketplace as a buyer or breeder.'
};

export default function SignupPage() {
  return <SignupForm />;
}
